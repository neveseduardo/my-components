import { Canvas, Meta, Story } from '@storybook/addon-docs'
import GSpinner from './Spinner.vue'

<Meta
    title="Spinner"
    component={ GSpinner }
/>

export const Template = () => ({
    components: { GSpinner },
    template: `<g-spinner />`,
})

# Spinner

Assim é o comportamento padrão do G-Spinner.

<Canvas>
    <Story 
        name="Spinner"
    >
        { Template.bind({}) }
    </Story>
</Canvas>
