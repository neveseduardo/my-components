import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { action } from '@storybook/addon-actions'
import GSwitch from './Switch.vue'

<Meta
    title="Switch"
    component={ GSwitch }
    argTypes={{}}
    parameters={{
        design: {
            type: 'figma',
            url: 'https://www.figma.com/file/hlesN40xogvZ32AQKrHw4D/Style-Guide?node-id=8291%3A4294',
        },
    }}
/>

export const Template = (args, { argTypes, updateArgs }) => ({
    props: Object.keys(argTypes),
    components: { GSwitch },
    template: `<g-switch v-bind="$props" @input="handleInput" />`,
     methods: {
        handleInput(value) {
            updateArgs({ ...args, value })
        },
    },
})

# Switch

Assim é o comportamento padrão do G-Switch.

<Canvas>
    <Story 
        name="Switch"
        args={{
            input: action('input'),
            value: false,
            text: { active: 'Sim', inactive: 'Não' },
        }}
    >
        { Template.bind({}) }
    </Story>
</Canvas>

> Modifique as propriedades abaixo, para ver todas as possibilidades de utilização deste componente.

<ArgsTable story="Switch" />
